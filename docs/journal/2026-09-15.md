# 2026-09-15 · nexa-ui journal

## 2차 (win · nexa-sql 세션) — nexa-fs · nexa-dlg::FilePicker · EditState 다중 선택 · TextBox 붙여넣기/더블클릭/탭 폭

- **`nexa-fs`**(신규 · 의존 0): `Entry`/`list(dir, show_hidden)`(Windows hidden 속성 · unix 점 파일) · `natural_cmp`/`sort`(폴더 먼저) · `places()`/`drives()`(Windows A:~Z: 프로브 · macOS `/Volumes` · Linux `/media/$USER`·`/mnt`) · `naming::validate`(3-OS 공통 집합) · `path::{display, resolve, expand_env, parent_chain}` · `local_time`(Windows `SystemTimeToTzSpecificLocalTime` · unix `localtime_r`) · `fmt_size`. dir2 `nexa-vfs`/`nexa-tree`/`pathinput` 추출.
- **`nexa-dlg`**(신규): `FilePicker{Open|Save}` — 컨트롤 조립 · `PickerLabels` 주입 · 정렬 헤더 클릭(제목에 ▲▼) · 필터 콤보 · 숨김 체크 · 새 폴더(중복 이름 (n)) · 더블클릭(500ms)/Enter/Backspace · 경로 상자 Enter · 저장 덮어쓰기 2단 + 확장자 부여 + 이름 검증 · `take_action()` · 포커스 ≤1(`own_focus`) · 마우스 = 커서 아래 / 키 = 포커스 · 콤보 팝업 모달+통과. 창은 소비자 몫(nexa-sql `file_win.rs`).
- **`EditState`**: `extra: Vec<(anchor, caret)>` · `regions/carets/add_selection/set_regions/clear_multi/selected_text_multi` · `edit_regions`(전 구간 누적 편집) · ←/→ 전체 이동 · 되돌리기 스냅샷 포함.
- **`TextBox`**: `select_next_occurrence`(Ctrl+D) · `set_column_mode`+`column_regions`(Alt+Shift 열 드래그) · 다중 선택/캐럿 페인트 + 선택 행 거터 강조 · Esc 접기 · `paste` 탭→공백(`indent_spaces`) · `DOUBLE_CLICK_MS` 시간 판정 · 페인트 진입에서 `set_tab_cols(self.tab_size)` · `tab_size()/indent_spaces()`.
- 테스트: nexa-ctl 202 · nexa-fs 6 · nexa-dlg 3.

## 3차 (win · nexa-sql 세션) — 글리프 캐시 · 아이콘 사전 스케일

- `text.rs`: `GlyphKey{face, gid, size bits, sub(1/3px)}` → `Arc<GlyphBitmap{w,h,ox,oy,cov}>` · `Font.cache`(Arc<Mutex<HashMap>> · 복제 공유 · face 추가 시 비움 · `GLYPH_CACHE_MAX=8192`) · `draw_styled` = 캐시 조회 + `Surface::blend_mask`(faux 볼드 2회 · 이탤릭 행 전단) · `draw_text`는 `draw_styled`로 위임 · `glyph_cache_len()` 진단.
- `surface.rs`: `blend_mask`(행 인덱스 1회 · 255 = 덮어쓰기 · 0 건너뜀 · 정수 혼합) · `IconImage::resized`(bilinear) · `blend_image_scaled` 같은 크기 → `blend_image`.
- 영향: nexa-sql(탐색기·편집기·그리드 전부 텍스트 경로) · clip · beep(이관 시).

## 4차 (win · nexa-sql 세션) — ContextMenu 아이콘·단축키·하위 메뉴 · EditMenu 장식 주입 · FilePicker 부가 콤보

- `ctxmenu.rs`: `MenuIcon{w,h,alpha}` · `CtxItem::Item{icon, shortcut, children}` + `submenu()/with_icon()/with_shortcut()` · 아이콘 칸 전 행 예약 · 단축키 우측 정렬 · `child: Option<Box<ContextMenu>>`(hover/→/클릭 열기 · ←/Esc 닫기 · 자식 선택 전파 · 오른쪽 넘침 시 왼쪽) · `bounds()` = 자식 합집합 · 테스트 4종(클릭 펼침·전파 / 키보드 →Enter← / hover 이동 / 아이콘 칸 폭).
- `editmenu.rs`: `EditMenuDecor{icons, shortcuts}` + `set_edit_menu_decor`(thread_local · 호스트가 키맵 변경 때 다시).
- `toolbar.rs`: hover/pressed 배경 제거(아이콘 색·눌림 1px만) · `textbox.rs`: 줄 넘김 선택 = 글자 끝 + 공백 한 칸.
- `nexa-dlg`: `set_extra(label, items, selected)` / `extra_value()` — 버튼 줄 확인 왼쪽에 콤보(모달 팝업 규칙 동일). 영향: nexa-sql.

## 5차 (win · nexa-sql 세션) — OS 아이콘 서비스(비동기 · 캐시) · FilePicker 아이콘/종류 이름 · 콤보 뷰포트

- **요청**(사용자): 파일 대화상자에 OS 폴더/파일 이미지(탐색기처럼) · 세 OS에서 OS 제공 이미지 최대 활용 · 성능 영향 0 · 확장성 · 메모리 적게 · 확장자 콤보가 창 밖으로 잘려 선택 불가.
- **실측**: `SHGetFileInfoW` 아이콘 ≈ 12ms/확장자 · 종류 이름 ≈ 2ms · 경로(홈) ≈ 17ms(`measure_lookup_cost` · `--ignored`). 동기 호출이면 폴더 하나에 수백 ms → **워커 스레드 + 전역 캐시**로.
- `shell.rs`: `RgbaIcon` · `IconKey{Kind{ext,is_dir}|Path}` · `IconService::global()`(`icon`/`kind_name` → `Lookup{Ready(Option)|Pending}` · `version()` · `pending()` · 상한 512 · 실패 기억 · `imp::SUPPORTED` false면 스레드 없이 `Ready(None)`) · Windows `imp`(HICON → 32bpp DIB · 알파 없으면 마스크 · GDI 객체 전부 해제 · STA COM 1회) · 테스트 `service_never_blocks_and_settles`(첫 조회 < 5ms · 5초 안 도착).
- `nexa-dlg`: `icon_for/kind_icon/path_icon`(Pending = 폴백 그림 `fallback_icon`) · `apply_icon_updates`(버전 변화 시 `TreeModel.roots[i].image`·`cells[2]` 제자리 교체 · 장소는 재구성 + 선택 보존) · `animating()`에 `icons_pending()` 포함(호스트 타이머 유지) · `set_viewport_bottom`(필터·부가 콤보 — 창 바닥 넘으면 위로 펼침).
- `tree.rs`: 16px급 원본은 원본 크기로 그린다(`max(LEADING_ICON, native)`).
- docs/20 §2-1(전략 표) · D-6 ⓑ 확정 · TODO F-8.

## 6차 (win · nexa-sql 세션) — FilePicker 결합 정렬 · 컬럼 이동 · 기본 순서

- **요청**(사용자): 파일 다이얼로그도 컬럼 Shift+클릭 · 컬럼 이동 · 순서 = 파일명·크기·일시·유형.
- `nexa-fs::sort_by(entries, &[(SortKey, bool)])`(폴더 먼저 · 키 순차 · 동률 = 이름).
- `FilePicker`: `sort_keys`(클릭 = 단일 3단 ▲→▼→해제 · Shift = 추가/방향/제거 · 배지 ▲1 ▼2 — 결과 그리드 규약) · `col_order: [2,1,3]`(논리 1=수정 2=크기 3=유형 · 이름 열 고정) · `hdr_drag`(4px 이상 = 이동 · 드롭 위치 = 셀 중앙 기준 · 끄는 열 선택색 + accent 드롭선) · `header_cells/logical_col/drop_pos_at/toggle_sort`.

## 7차 (win · nexa-sql 세션) — 경로 바(dir2 이식) · shell: 별칭 · 폴더 트리

- **검토**(사용자 "nexa-dir2 기능을 크로스플랫폼에 적용할 수 있는지"): `pathbar.rs`(위젯 · FS 비종속) · `nav.rs`(순수) · `pathinput.rs`(순수 · 이미 `path::expand_env`로 이식) = 그대로 이식 가능. `shellpath.rs`만 `windows` crate 의존 → nexa-ui 규칙(crate 0)대로 **raw FFI**(`SHParseDisplayName` · `SHGetPathFromIDListEx` · `CoTaskMemFree`)로 옮기고, macOS/Linux는 `shell:` 이름 공통 표(home/desktop/documents/downloads/startup/common startup/appdata → XDG·Library)로 같은 문법을 받는다.
- `nexa-fs`: `History`(dir2 nav.rs) · `shell::resolve_alias`(Windows 셸 정식 해석 → 폴백 표 · 가상 폴더 = None) · `path::expand_env`에 PowerShell `$env:NAME`/`${env:NAME}`(대소문자 무시) · `path::resolve`가 `shell:`을 먼저 본다 · COM 초기화 = **스레드별**(전역 Once는 첫 스레드만 초기화되어 UI 스레드의 `SHParseDisplayName`이 실패했다) · 테스트 `shell_aliases_resolve_like_explorer`.
- `FilePicker`: ⌂/←/→/↑ 버튼(뒤로/앞으로는 히스토리 상태로 활성) · **브레드크럼**(`parent_chain` · 끝 정렬 · 앞 넘침 `…` · hover · 마지막 = 현재 비활성 · 세그먼트 클릭 = 이동) · 빈 곳 클릭/우클릭 = 편집(기존 `TextBox` 전체 선택 · Enter = `resolve` → 이동/파일 확정 · Esc = 취소 · 밖 클릭 = 취소) · `popup_open()`이 편집 중을 포함(호스트 Esc 가드).
- **폴더 트리**(사용자 "트리뷰 기능도"): 장소 사이드바의 홈·문서·다운로드·드라이브·최근이 모두 **지연 로딩 폴더 노드**(`cells[0]` = 경로 · 자리표시 자식 → 펼칠 때 `list().filter(is_dir)` · 비면 자식 0 = 글리프 없음 = dir2 X-43) · 셰브론 클릭은 펼침만(이동 없음 · Explorer 탐색 창 관례) · 행 선택 = 이동. nexa-ctl `TreeView`에 지연 로딩 훅이 없어 호스트가 펼친 뒤 자리표시를 교체하는 방식 — `TreeView::on_expand` 콜백 승격은 F-3.

## 8차 (win · nexa-sql 세션) — dir2 셰브론 공용화 · 목록 트리 · 우클릭 메뉴 · 가상 최상위

- **요청**(사용자): dir2 확장/축소 모양 그대로 · 좌측 트리 유지 + 우측도 트리·우클릭 등 전부 · 집 아이콘/최상위 이동에 dir2 가상 최상위 개념 · 맥/리눅스 적용 가능성.
- `nexa-ctl`: `controls::draw_chevron_90`(nexa-sql 탐색기의 것을 부품으로 승격 · dir2 MDL2 글리프는 Windows 글꼴이라 코드 도형으로 같은 모양) — `TreeView`/`TreeGrid` 셀이 모두 사용(크기 = 글꼴 높이 · 접힘 흐림 · 펼침/hover 본문색). `draw_chevron_right/down`은 남김(콤보 등).
- `nexa-dlg`: 오른쪽 목록 행 = `make_row`(보이는 셀 뒤 **숨은 셀** `CELL_PATH/CELL_KIND/CELL_EXT` — 그리드는 열 수만큼만 그린다) · 폴더 = 자리표시 자식 → `lazy_load_grid`(하위 폴더 + 필터 파일 · 같은 정렬) · 셰브론 클릭 = 펼침만 · 더블클릭/Enter = 진입/확정(펼친 하위 파일은 그 폴더로 옮긴 뒤 확정) · 아이콘/종류 도착 갱신은 재귀 · `shown` 매핑 제거. **우클릭 메뉴**(nexa-ctl `ContextMenu` · 열기/경로 복사/이름 복사/새 폴더/새로 고침/숨김 ✓ · 클립보드는 `PickerAction::CopyText`로 호스트에).
- `nexa-fs`: `VIRTUAL_ROOT = "::PC::"` · `is_virtual_root` · `drive_entries()`(`list()`가 가상 루트면 이것) · `parent_chain` 맨 앞 = PC. **3-OS**: `drives()`가 이미 Windows `A:~Z:` / macOS `/`+`/Volumes/*` / Linux `/`+`/media/$USER/*`+`/mnt/*`를 주므로 같은 개념이 그대로 적용된다(볼륨 이름 = 폴더 이름). 대화상자: 루트에서 ↑ = PC(드라이브 선택 유지) · 브레드크럼 첫 조각 = "내 PC" · 사이드바 그룹 행 클릭 = PC · 드라이브 행 = 경로 아이콘 + 종류 "드라이브".

## 9차 (win · nexa-sql 세션) — 빈 폴더 셰브론 억제

- `nexa-fs`: `has_visible_child(dir, show_hidden, exts)` · `has_subfolder(dir, show_hidden)` — `read_dir` 첫 일치에서 반환 · 숨김 규칙 동일 · 실패 = `true`(글리프 유지).
- `FilePicker`: `make_row`/`folder_node`가 생성 시 판정 → 자리표시 자식 유무 = 셰브론 유무 · 숨김 체크 토글 = `reload` + `rebuild_places`.

## 10차 (win · nexa-sql 세션) — 점 파일 토글

- `nexa-fs`: `list_opts(dir, show_hidden, show_dot)`(`list` = show_dot true) · 프로브 2종도 같은 인자. 점 파일 = 이름 `.` 시작(Windows 숨김 속성과 독립).
- `FilePicker`: `dot_chk`(기본 켬 · 숨김 체크 오른쪽) · 우클릭 메뉴 `dot` · 토글 = `reload` + 사이드바 재구성 · `PickerLabels.show_dot`.

## 11차 (win · nexa-sql 세션) — 열 합 폭 선택 · 빈 공간 메뉴

- `tree.rs`: `TreeControl::hit_width()`(기본 전폭) → `row_hit`가 그 밖은 `None` · `TreeGrid.fit_columns`면 열 합 − 가로 스크롤 · 선택/hover 사각형도 같은 폭.
- `nexa-dlg`: 목록 `set_fit_columns(true)` · `open_menu` — 행 위(`menu_row` Some) = 열기·경로 복사·이름 복사 + 폴더 메뉴 · 빈 공간 = 폴더 메뉴만.

## 12차 (win · nexa-sql 세션) — 백그라운드 열거 · 회수 규칙

- **요청**(사용자): 파일 탐색이 성능·메모리에 영향 없게 · 사용 후 회수 · 스레드에서 동작.
- `nexa-fs/lister.rs`: `ListOpts{show_hidden, show_dot, exts, dirs_only, batch, skip_probe}` · `ListHandle::start(dir)`(요청당 스레드 · `read_dir` 스트리밍 · `Batch(256)` → `Probe(path, has_any, has_dir)` → `Done`) · `ListHandle::probe(paths)`(경로 목록 셰브론 판정만) · `try_recv`(블록 0) · **`Drop` = 취소**(다음 경계에서 종료 · 미전송 배치 폐기) · 스레드 생성 실패 = 동기 폴백 · `entry_of` 공유(동기 `list_opts`와 규칙 동일) · 테스트(600 파일 + 필터 + 프로브 + 취소 플래그).
- `shell.rs`: 아이콘 워커 `recv_timeout(30s)` → 유휴면 `tx = None` 후 종료(같은 잠금 아래 `try_recv`로 유실 방지).
- `nexa-dlg`: `loader`(현재 폴더) · `sub_loaders: Vec<SubLoad>`(지연 펼침 · 그리드/사이드바 · 접히면 버림) · `probe`(장소) · `probes: HashMap<Path,(bool,bool)>` · `poll_loaders()`(틱 · 프레임당 8개) · `apply_probes()`(자리표시 자식 제거 = 셰브론 없음) · `apply_sub_loader()`(배치마다 정렬 교체) · `select_after`(새 폴더·↑ 복귀) · 동기 `list_opts`/`has_visible_child`/`has_subfolder` 호출 전부 제거 · `animating()`에 `loading()` 포함.

