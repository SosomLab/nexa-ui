# 2026-09-12 — 저장소 생성 · 추출

## 1차 (mac) — nexa-clip → nexa-ui 추출

**왜 clip이 원본인가**: dir2(`nexa-gui`, Win32 전용 앱 속 플랫폼 중립 크레이트) → beep(`nbeep-ctl`, 3-OS CPU 래스터) → clip(`nclip-ctl`, beep 포크 + `tokens.rs` 디자인 시스템(DR-27) + `view_mode` + `listedit` + hover fade(DR-34)). 가장 많은 컨트롤·가장 최근 수정(09-08).

**결합도 실측**: `grep use nclip_` → `nclip-ctl`은 `raster.rs` 1줄(`use nclip_gfx::{Font, Surface, TextStyle}`)만 외부 크레이트. `nclip-gfx`는 `ab_glyph`뿐. `nexa-conf`는 0.

**절차**: `cp -R` 3 크레이트 → `sed` 식별자 치환(`nclip_gfx`·`nclip-gfx`·`nclip_ctl`·`nclip-ctl`) → 주석 속 앱 이름 일반화(`nclip-plat`→`<app>-plat` 등 · 출처 메모는 유지) → 워크스페이스 `Cargo.toml`(lints·profile은 clip 것 계승, release 프로필은 소비자 몫) → `rust-toolchain.toml`·`scripts/check-3os.sh`·`ci.yml`·`docs/16` 복사.

**실측**: fmt ✓ · clippy `-D warnings` ✓(5.8s) · 테스트 gfx 14 / ctl 165 / conf 10 = **189 green**.

**남긴 것**: dir2 `widgets`(7,648 LOC)는 `DrawCtx` 세대 차(`select_font(slot,bold,italic)`·`term_text`·`draw_image`·`draw_icon`)라 그대로 못 넣는다 → D-3(어휘 합집합) 뒤 U-2.
