# 2026-09-18 — 32차(mac) · TabBar 앞 표식 · Toolbar 항목 자리 · TextBox 단일행 표시 규칙

## 32차 — nexa-sql 세션 모드(DR-34) 지원 부품 (nexa-sql 사용자 09-18)

- **`TabBadge`**(`None`·`Link`·`LinkOff`·`Shared`) + `TabBar::set_badges/badge/badge_rect_of` — 탭 제목 **앞**의 이미지 버튼 모양 표식(둥근 상자 + 플러그 글리프 · hover/눌림 상태 레이어 · 끊김 = 흐림 + 사선 · 공유 = 중립색 · 전용 = 강조색). 폭을 차지하고(`BADGE_BOX` 18) 핀 점보다 앞. 히트 존 `Zone::Badge` → **좌클릭 = `TabAction::Badge(i)`(프레스-해제 같은 표식일 때 · 탭 전환·드래그 아님) · 우클릭 = `TabAction::BadgeContext(i)`**(본체 우클릭 `Context`와 구별). `tab_index_at`은 표식 위도 그 탭. 테스트 +1.
- **`Toolbar::item_rect(id)` · `ToolDock::item_rect(id)`**(플로팅 그룹 = None) — 호스트가 드롭다운 메뉴를 버튼 아래에 붙인다(nexa-sql Disconnect ▾).
- **TextBox 단일행**(사용자 캡처 3장): ① **포커스 없는 상자는 앞부분부터**(값을 넣으면 캐럿이 끝이라 긴 경로의 꼬리만 보이던 것 · 포커스를 받으면 종전대로 캐럿 추종 · 사용자가 휠로 옮긴 위치는 유지) ② **가로 스크롤 표시는 스크롤하는 동안만** — `ml_bars.show()` + 오버레이 스크롤바와 같은 자동 숨김 시계(`scroll::hide_delay_ms`) · `is_animating()`이 표시가 떠 있는 동안 true(호스트 틱 예약). 09-17에 "넘치면 항상 표시"로 넣었던 것이 원인(꺼진 것이 아니라 자동 숨김을 안 붙였음). 테스트 +1(`unfocused_shows_start_and_hbar_auto_hides`).
- 테스트 248 green · clippy 0.
