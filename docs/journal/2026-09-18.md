# 2026-09-18 — 32차(mac) · TabBar 앞 표식 · Toolbar 항목 자리 · TextBox 단일행 표시 규칙

## 32차 — nexa-sql 세션 모드(DR-34) 지원 부품 (nexa-sql 사용자 09-18)

- **`TabBadge`**(`None`·`Link`·`LinkOff`·`Shared`) + `TabBar::set_badges/badge/badge_rect_of` — 탭 제목 **앞**의 이미지 버튼 모양 표식(둥근 상자 + 플러그 글리프 · hover/눌림 상태 레이어 · 끊김 = 흐림 + 사선 · 공유 = 중립색 · 전용 = 강조색). 폭을 차지하고(`BADGE_BOX` 18) 핀 점보다 앞. 히트 존 `Zone::Badge` → **좌클릭 = `TabAction::Badge(i)`(프레스-해제 같은 표식일 때 · 탭 전환·드래그 아님) · 우클릭 = `TabAction::BadgeContext(i)`**(본체 우클릭 `Context`와 구별). `tab_index_at`은 표식 위도 그 탭. 테스트 +1.
- **`Toolbar::item_rect(id)` · `ToolDock::item_rect(id)`**(플로팅 그룹 = None) — 호스트가 드롭다운 메뉴를 버튼 아래에 붙인다(nexa-sql Disconnect ▾).
- **TextBox 단일행**(사용자 캡처 3장): ① **포커스 없는 상자는 앞부분부터**(값을 넣으면 캐럿이 끝이라 긴 경로의 꼬리만 보이던 것 · 포커스를 받으면 종전대로 캐럿 추종 · 사용자가 휠로 옮긴 위치는 유지) ② **가로 스크롤 표시는 스크롤하는 동안만** — `ml_bars.show()` + 오버레이 스크롤바와 같은 자동 숨김 시계(`scroll::hide_delay_ms`) · `is_animating()`이 표시가 떠 있는 동안 true(호스트 틱 예약). 09-17에 "넘치면 항상 표시"로 넣었던 것이 원인(꺼진 것이 아니라 자동 숨김을 안 붙였음). 테스트 +1(`unfocused_shows_start_and_hbar_auto_hides`).
- 테스트 248 green · clippy 0.

## 33차 — Toolbar 드롭다운 ▾ 클릭 구별 (nexa-sql 사용자 09-18)

- `ToolItem::with_dropdown` 항목의 **▾ 영역(오른쪽 DROP_W)** 에서 놓으면 클릭 id가 `"<id>#drop"`으로 온다 — 본체 = 기본 동작(nexa-sql Disconnect = 모두 해제) · ▾ = 목록(세션 관리자). 테스트 4 green.

## 34차 — CtxItem ✓ 체크 표시 (nexa-sql 사용자 09-18)

- `CtxItem::with_mark(bool)` — 아이콘 칸에 ✓(`draw_check_mark` 재사용) · 하나라도 있으면 전 행이 칸을 비워 글자가 세로 정렬 · `ui.menu_icons`와 무관(라디오 목록의 "지금 것"). 기존 `with_checked`(토글 스위치 모양)와 구별.

## 35차 — TabBadge 모양 (nexa-sql 사용자 09-18)

- `draw_badge`: 바탕 둥근 사각형 공통 · 연결됨(`Link`/`Shared`) = `switch::ON_GREEN` 글자 **P**/**S**(굵게 · 상자 가운데) · `LinkOff` = 흐린 플러그 + 사선.

## 36차 — Toolbar 툴팁 클램프 (nexa-sql 사용자 09-19)

- `Toolbar::paint_tooltip_in(clamp_x)` + `ToolDock::paint_tooltip`이 **도크 폭(= 창 폭)** 을 넘긴다 — 오른쪽 정렬 그룹의 툴팁이 창 밖으로 잘리던 것(종전 `anchor.right()+240` 허용). 혼자 있는 바(플로팅)는 종전 규칙.

## 37차 — TabBadge 색 (nexa-sql 사용자 09-19)

- `Shared` = `switch::ON_GREEN`(초록) · `Link`(전용) = `tab_accent`(파랑) · `LinkOff` = `text_dim`.

## 38차 — TextBox 바뀜 띠 · Button Accent (nexa-sql T-131 · 09-19)

- `TextBox::set_modified(bool)` — 입력란 왼쪽 안쪽 2px 강조색 띠(편집기 줄 변경 표시와 같은 어휘 · 포커스 링과 겹치지 않음). 폼이 저장본과 다른 칸에 켠다.
- `ButtonTone::Accent` — 대기 중 동작(예: `Save •`)을 강조색으로. 툴바 Commit 배지와 같은 문법.

## 39차 — ToolDock 고스트·다중 행·Esc · TextBox ↑/↓ 줄 끝 · polyline 클립 · CtxMenu 체크박스 (nexa-sql 09-19)

- **`ToolDock`**: 그룹 드래그 = 4px 임계 → **고스트**(`Toolbar::paint_offset` · 호스트가 팝업 층에서 `paint_drag_overlay`) + 제자리 강조 자리 표시(라이브 미리보기) · **다중 행**(`row_of` · 도크 바로 아래 한 행 폭 = 새 행 · `DockAction::Resized` · 빈 행 접힘 · `DockLayout::rows` · 문자열 `a,b;c` = `;` 다음 행 · 종전 값 호환) · 떼어 내기 = 도크(+새 행 영역) 밖 `TEAR_PX` · **`cancel_drag`(Esc)** · `is_dragging`. 테스트 4.
- **`TextBox::ml_move_vert`**: 첫 줄 ↑ = 줄 맨 앞 · 마지막 줄 ↓ = 줄 끝(Sublime/VS Code/mac 관례).
- **`DrawCtx::polyline_clipped`** + `draw_chevron_90_in(clip)`: 부분적으로 잘린 행의 셰브론을 보이는 만큼만(nexa-sql 탐색기 반 줄 스크롤 결함).
- **`CtxMenu` 토글 항목(`with_checked`) = 일반 체크박스**(`draw_checkbox_glyph`) — 스위치 글리프는 켜짐이 잘 안 보였다(사용자).

## 40차 — TextBox 성능(입력 지연) · 목표 열 · 경고 띠 (nexa-sql 09-19 · [nexa-sql 55](../../../nexa-sql/docs/55-editor-input-latency.md))

- **`EditState::rev`**(본문 변경 세대) · **`chars()`**(슬라이스) — 호출자가 `text()`로 String을 다시 만들지 않게. `TextBox::text_rev`/`chars`.
- **`paint_multiline`**: 내용 폭을 세대·글꼴·wrap 키로 캐시(`ml_width_cache` · 종전엔 전 줄을 매 프레임 측정) · `logical_lines` = `(usize, &str)` 슬라이스(줄마다 String 없음 · wrap 아니면 1회) · 본문 String은 preedit 있을 때만. `ml_move_vert`/`ml_line_edge`/`caret_column`/스마트 Home/⌘End = 슬라이스.
- 벤치 `examples/bench_editor`(dev-dep `nexa-font`): 2 MB 본문 페인트 195 → 12.9 ms · ↓ 7.46 → 0.000 ms · 200 KB 21.8 → 4.9 ms.
- **목표 열**(`goal_x`/`goal_col`): ↑/↓ 연속 이동은 처음 x를 유지(빈 줄·짧은 줄 지나도) · 다른 이동·편집이면 초기화.
- **`TextBox::set_warning`**: 경고색 띠(빠진 필수 칸 · 바뀜 띠보다 우선).
- 후속(09-19 win · 41차): **Toolbar 글리프 항목 비활성 흐림** — `ToolIcon::Glyph`가 `enabled`·`tone`을 무시하고 늘 본문색이라 `.disabled()` 항목이 켜진 것처럼 보였다(nexa-sql 결과 도구줄 + − ⧉ ✓ ✕ Σ) → 마스크 항목과 같은 색 규칙(비활성 = `text_dim` · hover/pressed = accent · 아니면 색조).
- 후속(09-19 win · 41차): **TreeControl ← 키** — 펼쳐진 행이면 접고, 잎/접힌 행이면 **상위 폴더로 이동**(위쪽 첫 얕은 행 · 루트는 그대로 · TreeView/TreeGrid 공용 → nexa-dlg 파일 대화상자·설정 창 트리) · 테스트 `keyboard_navigates_and_expands` 확장(nexa-sql 사용자 09-19).
- 후속(09-19 win · 41차): **타입어헤드 부품** `typeahead.rs`(`TypeAhead` 버퍼+한글 조합+타임아웃 · `TypeAheadFilter` · `find_prefix`/`find_prefix_rev` · `HudPos`+`paint_hud`) + `hangul.rs`(두벌식 직접 조합기 · `jamo_from_qwerty` — nexa-beep `nbeep-ui` 이식) · 테스트 17 · 매칭은 소비 위젯의 라벨 함수(nexa-sql 오브젝트 탐색기 첫 사용자 · 사용자 09-19).
- 후속(09-19 win · 41차): **Switch/Checkbox 빈 bounds = 보이지 않음** — 트랙·상자가 고정 크기라 호스트가 화면 밖 컨트롤을 `Rect::default()`로 두면 (0,0)에 그려졌다(nexa-sql 설정 창 왼쪽 위 스위치 조각 · 사용자 09-19) → `paint` 첫 줄 `is_empty` 가드. 규칙: 고정 크기 글리프를 그리는 컨트롤은 빈 bounds에서 아무것도 그리지 않는다.
- 후속(09-19 win · 41차): **`PositionDropdown`**(`controls/posdrop.rs`) — 선택 위치 타일 + ▾ 머리 · 팝업 = `PositionPicker` 3×3 · `paint_popup`/`take_changed`/`is_open` · `posgrid::paint_cell`·`box_rect_in` 공개 · `HudPos::code/from_code` · `set_max_bottom`(목록/창 바닥을 넘으면 머리 **위로** 펼침 — 창 밖으로 잘리던 것) · 테스트 3(nexa-sql 설정 창 타입어헤드 HUD 위치 · 사용자 09-19).
- 후속(09-19 win · 42차): **색 대비 순서** — `contrast_order(colors)`(첫 색 고정 · 이웃 쌍(끝→처음 포함) 최소 대비가 가장 큰 순열 · 8색 이하 전수 · 초과 = 탐욕) · `color_contrast`(색상각 0.6×채도 + 온도 다름 0.2 + 밝기 차 0.2) · `is_warm`(색상각 < 90° 또는 ≥ 300°). `Theme.rainbow`(어두움·밝음)를 그 순서로 저장(금 → 파랑 → 자홍 → 초록 → 주황 → 보라) — 따뜻함↔차가움 교대 · 테스트 `contrast_order_separates_neighbours` · `theme_rainbow_is_already_contrast_ordered`(색을 바꾸면 새 순서를 알려 준다) · 272 green · clippy 0(nexa-sql 사용자 09-19).
- 후속(09-19 win · 43차): **`SyntaxSpec.numbers`** — 숫자 리터럴을 칠할지(기본 켬 · `SyntaxSpec::plain()` = 끔 · `.nexa-syntax`의 `numbers = off`). Plain Text 읽을거리(nexa-sql 확장 상세 탭)의 버전 번호·날짜가 초록으로 칠해지던 것 · 테스트 `plain_text_has_no_number_tokens` · 273 green(nexa-sql 사용자 09-19).
- 후속(09-19 win · 44차): **외부 변경 처리 부품 셋**(nexa-sql docs/58) — ① `nexa-ctl merge3(base, ours, theirs) -> Option<Merge3{text, conflicts, applied, applied_lines}>`: Myers O(ND) 줄 정합(공통 앞뒤 제거 · 되짚기 기록은 단계별 창만 = D² · 편집 거리 1,500 상한 → `None`) · 안정 줄이 사이에 없으면 한 덩어리(git보다 보수적) · 테스트 5 ② `TextBox::replace_all_undoable`: 달라진 가운데만 한 번의 교체 · 캐럿은 같은 글자에 · 사용자가 보던 스크롤 유지 · Ctrl+Z 한 번 = 교체 전 ③ `nexa-fs watch`: `FileSig`(크기·mtime·Unix dev/inode) · `content_hash`(FNV-1a) · `StatWatch`(스레드 `file-watch` · 서명이 연속 두 번 같을 때 읽기 · 같은 열쇠 요청 합침 · 상한 초과 = `Unreadable` · 없어짐 = `Missing`) · 테스트 2. clippy 0 · nexa-ctl 279 · nexa-fs 13 green(nexa-sql 사용자 09-19).
- 후속(09-19 win · 45차): **큰 파일 성능**(nexa-sql docs/59 §1) — `bench_editor <줄 수> <기능>`으로 기능별·입력 비용을 재어 병목 다섯을 고쳤다: ① `HlStateCache`(구문 줄 시작 상태 · 종전 = 매 프레임 0번 줄부터 재토큰화 87 ms · 줄 해시로 앞부분을 살리고 필요한 줄까지만) ② `RowWidthCache`(행별 폭 · 종전 = 편집마다 전 줄을 폰트 엔진으로 200 ms · 바뀐 가운데 행만) ③ `auto_close`/`backspace_pair`/`outdent_on_close`/`room` 등 키 경로의 `edit.text().chars().collect()` 제거(24 → 0.1 ms) ④ `MlTextCache`(본문 문자열 + 줄 표를 한 번 훑어 생성 · 캐럿 줄 = 이분 탐색) ⑤ 줄끝 표시 = 버퍼를 빌림. 4만 줄·전 기능: 유휴 그리기 78 → 3.3 ms · 입력 209 → 16 ms. **되돌리기**: `Snap.buf`가 묶음마다 전체 복사이던 것 → `SnapBuf::{Full, Delta}`(맨 위만 전체 · 새 묶음 기록 때 차이로 접음 · undo/redo가 역차이를 주고받음 · `history_chars`/`clear_history`). API: `TextBox::release_caches`·`clear_history`·`approx_bytes`·`paint_work`. **nexa-dlg**: 덮어쓰기 확인 = `TimeoutButton` 무장(첫 누름 = 빨간 타이머 · 시간 안의 두 번째 = 확정 · Enter 동일 · `set_overwrite_confirm_ms`). 281 green · clippy 0(nexa-sql 사용자 09-19).
- 후속(09-19~20 win · 46차): **되돌리기 = 연산 기록**(nexa-sql [60](../../../nexa-sql/docs/60-undo-redo-redesign.md)) — `edit.rs` `SnapBuf` 폐기 → `Op{pos, remove_n, insert, insert_n}`/`Txn{id, ops, 캐럿·선택, bytes}` · 적용하면 역연산(되돌리기·다시 실행 = 한 함수) · **저장 = 지운 글자만** · 묶음 안 연산은 뒤에서 앞으로 · 오름차순·비겹침이면 한 번 훑기 · **본문 변경 단일 통로** `splice_rec`/`replace_many_inner`(`edit/ops.rs`의 흩어진 `buf.splice` 5곳 이관 — 일부가 `rev`를 안 올리던 결함 해소) · 코얼레싱(인접 타이핑 · Backspace/Delete 연속 · 단어 경계) · `begin_group`/`end_group` · `replace_many` · `insert_str` = splice 한 번(붙여넣기 O(n²) 제거) · 저장 지점 `mark_saved`/`is_saved`(상태 id · O(1)) · 바이트 예산 `set_history_budget`/`evict` · `set_read_only`. `TextBox`: `replace_many` · `begin_undo_group`/`end_undo_group` · `mark_saved`/`is_saved` · `set_history_budget` · `history_stats` · `set_read_only`. 벤치 `examples/bench_undo`: 붙여넣기 100 KB 34,074 → 1.0 ms · 모두 바꾸기 2,000건 15,447 → 3.4 ms(1단계) · 단어 6.79 → 1.66 ms. 테스트: 난수 속성(40×120 vs 스냅샷 모델) · 예산 · 묶음 · 읽기 전용.
- 후속(09-20 win · 46차): **`PreparedText` + `TextBox::set_prepared`** — 글자 버퍼 + 본문 문자열 + 논리 줄 표를 UI 스레드 밖에서 만들어(`Send`) 복사 없이 옮겨 넣는다(캐럿 = 처음 · 히스토리 비움 · 문자열은 `MlTextCache`가 그대로 씀) · `EditState::set_chars`. 65 MB: `set_text` 130~220 ms + 첫 페인트의 재조립 → UI 스레드 0.02 ms. 테스트 = `set_text` 경로와 같은 스냅샷(빈 글·끝 개행·빈 줄·한글) + `Send` + 같은 버퍼.
- 후속(09-20 win · 46차): **행 폭 고정폭 지름길** — `RowWidthCache`를 새로 채울 때(2,000행 이상) 글꼴이 고정폭이면(서로 다른 글자 400자 탐침 둘의 폭이 같다) 인쇄 가능 ASCII + 한글 음절(가–힣 · 폴백 글꼴도 탐침으로 확인)만 있는 행은 `글자 수 × 전진폭(소수)`으로 — 폰트 엔진 호출 0. 70만 줄 첫 페인트 3.5~3.8초 → 0.2초. 폭은 가로 스크롤 범위에만 쓰여 ±1px 무해 · 디버그 빌드는 지름길 첫 행을 실측과 대조(`debug_assert`). `bench_editor`: `set_text`/첫 페인트 시간 · `BENCH_ASCII` · `BENCH_PREPARED`.
- 후속(09-20 win · 47차): ★ **`TextBuf`**(`edit/textbuf.rs` · nexa-sql [59 §6](../../../nexa-sql/docs/59-large-file-handling.md) T-142) — UTF-8 갭 버퍼 + 줄 시작 표(줄마다 바이트·글자 시작) + 줄 변경 기록(`LineChange` · `epoch`). **좌표는 글자 인덱스**(바이트는 안에서만 · ASCII 줄 O(1) · 위치 캐시). `get`/`slice`/`slice_vec`/`iter_from`/`iter_rev_from`/`contains_in`/`starts_with_at`/`line_of`/`line_start`/`line_end`/`line_text`/`find`/`eq_str`/`byte_len`/`content_hash`/`splice`/`replace_many`/`set_string`/`adopt`/`shrink` · trait `CharSeq`(`[char]`·`TextBuf` — `word_boundary`/`subword_boundary` 제네릭) · `Hash64`. `EditState.buf: Vec<char>` → `TextBuf`(`chars()` 폐지 → `buf()`/`len()` · `chars_vec()`은 드문 명령·테스트용) · `splice_rec_str`(붙여넣기를 글자 배열로 풀지 않음) · 여러 곳 편집 64곳 이하는 뒤에서부터 한 곳씩. `edit/ops.rs` 36곳 · `textbox.rs` 26곳 이관(줄 도우미 = 줄 표 · 괄호 짝 = 반복자 · Ctrl+D = `find`). **그리기**: `Rows`(접지 않으면 버퍼의 줄 = 행 · 보이는 줄만 · 조합 중 글자는 캐럿 줄 하나) · `RowWidthCache`/`LineHlCache` = 변경 기록으로 갱신(전 행 해시·본문 재조립 제거) · 접기 모드는 종전 경로 · `MlTextCache` = 본문 문자열만. `PreparedText` = `TextBuf`(문자열 바이트 그대로). 벤치 70만 줄: 입력 157/184 → 3.0/4.1 ms · 그리기 12.8 → 2.1 ms · 첫 페인트 195 → 55 ms · 메모리 348 → 86 MB. 테스트: 난수 모델 대조(30×150) · 가장자리 · 반복자/구간 · 변경 기록 · 먼 조회 · `find` = `str::find` · 해시 = 갭 무관 · 그리기/조합(`rows_come_from_buffer_lines…`).
- 후속(09-20 win · 47차): **되돌리기 후속**(nexa-sql [60 §7](../../../nexa-sql/docs/60-undo-redo-redesign.md)) — `set_group_pause_ms`(쉬었다 치면 새 묶음) · `merge3::line_edits`(두 본문 → 바뀐 줄들만의 편집 목록 · 끝 개행/덧붙임/지움 · 14×14 테스트) → `TextBox::replace_all_undoable`이 최소 줄 편집을 `replace_many` 한 단계로 · **거대 편집 확인**(`set_giant_limit` · `giant_refused` — 지우는 동작만 되풀이로 확인 · 진행하면 `clear_history` · `take_giant_blocked`) · **기록 파일**(`export_history(max)`/`import_history` — `NSQU` · 판 · 본문 길이·해시 · 상태 id · 단계 · 꼬리 해시 · 어긋나면 통째 거부).
- 후속(09-20 win · 48차): CLAUDE.md **§3-1 세션 공통 규칙 · 편집기 코어 불변식** — 로컬 메모리·대화에만 있던 규칙을 저장소로(다른 PC·맥에서 그대로 · 원문 = nexa-sql docs/61): 글자 인덱스 좌표 · 본문 변경 통로 셋 · 본문 전체 복사 금지 · 줄별 캐시 = 변경 기록 소비자 · 난수 모델 대조 · 브랜치 흐름.

## 49차 (mac) — TextBox 한글 **앱 조합** · nexa-sys `input_source` (nexa-sql T-139 · 09-20 · [nexa-sql 62](../../../nexa-sql/docs/62-macos-input-and-present.md))

- 배경: macOS에서 winit 창이 시스템 한글 IME를 거치면 ① 입력란이 IME를 막 붙인 직후 첫 키가 조합 없는 자모로 흘러오고 ② 조합 확정 직후 첫 1바이트 글자의 keydown이 앱에 도달하지 않는다(nexa-beep H-14·H-26과 같은 뿌리 · nexa-sql에서 `가나다1234` → `ㄱㅏ나다234`로 재현). 목록 타입어헤드(71차 이식)는 IME를 끄고 앱이 조합해 이 문제가 없었다 → 같은 경로를 텍스트 상자 공통으로.
- **nexa-ctl `TextBox`**: 전역 스위치 `set_hangul_app_compose`/`hangul_app_compose`(기본 끔 — Windows/Linux 불변 · 테스트에서는 스레드별) · 자모 `Char` = `hangul::Composer`에 먹이고 확정 글자는 평소 `Char` 경로로(되돌리기 묶음·다중 캐럿 그대로) · 조합 중 글자 = 기존 preedit 표시(`set_preedit`) · `Char('\u{8}')` = 자모 단위 · 자모 아닌 글자·`Key`·마우스 누름·붙여넣기 등 = 먼저 확정(`hangul_flush`) · 마우스 이동·휠은 조합 유지 · `Control::set_focused(false)` = 확정 · 마스크(비밀번호) 칸은 조합하지 않음. `on_event` = 조합 경로 → `on_event_core`(종전 본문). 테스트 3(`가나다1234`·`가나다!@#$` 전량 · preedit/Backspace/포커스/도깨비불 · 스위치 끔 = 불변).
- **nexa-sys `input_source`**: `is_korean()`(Carbon TIS 입력 소스 id가 `com.apple.inputmethod.Korean…`) · `watch()`/`take_changed()`(분산 알림 `kTISNotifySelectedKeyboardInputSourceChanged` · 콜백은 깃발만) — 의존 0 · 수동 FFI(Carbon·CoreFoundation 프레임워크) · 다른 OS = `None`/no-op · 주 스레드 전용. 테스트 1.
- 검증: 워크스페이스 clippy 0 · 테스트 352 green · nexa-sql 자동 실기(9개 입력란).

## 50차 (mac) — nexa-sys `layer_present` = macOS IOSurface 화면 내보내기 (nexa-sql T-147 · D-133 ② · 09-21 · [nexa-sql 62 §2](../../../nexa-sql/docs/62-macos-input-and-present.md))

- **왜**: softbuffer 0.4 CoreGraphics 뒷단 = 프레임마다 새 버퍼(레티나 메인 창 21 MB) + DeviceRGB `CGImage` → CoreAnimation이 프레임마다 전체를 CPU(vImage)로 색 변환(present 37 ms).
- **무엇**: `LayerPresenter::new(ns_view)` → `resize(w, h, scale)` → `frame()`(`pixels_mut` = `0x00RRGGBB` 폭 × 높이) → `present()`. 표면 풀(≤ 3 · `IOSurfaceIsInUse`로 합성기가 쥔 장을 피함) · sRGB 색 공간 태그(`kIOSurfaceColorSpace`) = 색 맞춤은 합성기 · 레이어 배치는 softbuffer와 같게(원점 왼쪽 위 · `kCAGravityTopLeft` · 암묵 애니메이션 없음) · 의존 0 · 수동 FFI(`objc_msgSend` 서명 캐스팅 · CoreFoundation · CoreGraphics · IOSurface · QuartzCore) · macOS 밖 = `new`가 늘 `None`인 같은 모양.
- **실측으로 잡은 함정 둘**: ① 'BGRA' 표면의 **알파 0 = 투명**(`opaque`는 힌트일 뿐) → present 때 0xFF 채움(행 복사와 한 번에) ② **행 바이트를 `폭 × 4`로 강제하면 표면은 만들어지는데 합성기가 그리지 않는다**(Intel i9 + AMD · 폭 2750 → 11000바이트 = 빈 창 · OS 기본 정렬 11008 = 정상) → 행 바이트는 OS에 맡기고, `폭 × 4`와 다르면 중간 버퍼에서 행 단위로 옮긴다(같으면 표면에 직접 = 복사 0).
- **수치**(nexa-sql Release · 2750×1890px): present 36.2 → **2.9 ms** · 프레임 51.2 → 16.0 ms · 유휴 CPU 30초당 3.1 → 0.9초 · 풋프린트 74 → 94 MB(표면 3 + 중간 버퍼).
- 남은 것: `nexa-gfx::Surface`에 행 간격(stride)을 주면 중간 버퍼(21 MB · 복사 1)가 사라진다 · 유휴 때 풀 줄이기.
- 검증: fmt ✓ · clippy 0 · 테스트 352 · `check-3os.sh` ✓.

## 51차 (win) — 팝업 배치 규칙 · 탭 끌기 고스트 · 편집기 밖 끌기 선택 · 폴더 고르기 · 가린 입력란 = 비밀 값 (nexa-sql 89차 · 09-21 · [nexa-sql journal](../../../nexa-sql/docs/journal/2026-09-21.md))

- **팝업 배치 규칙**(`geom`): `place_popup`(정방향 → 반대쪽 → 밀어 넣기 · 가장 가까운 자리) · `nudge_into` · `popup_host(host, 표면)` · `DrawCtx::surface_size()` — `ContextMenu`(하위 메뉴 포함) · 툴팁(세로 뒤집기) · `Combo` 드롭다운이 **그리는 시점에 표면 크기를 배워** 스스로 창 안으로 들어온다(호출자가 영역을 잘못 넘겨도 잘리지 않는 안전망). CLAUDE.md §3-0 규칙으로 올림.
- **대상을 가리지 않는 메뉴**: `geom::place_popup_beside`(대상 행 바로 아래 → 없으면 바로 위 → 둘 다 안 되면 일반 규칙 · 가로는 누른 x에서) · `ContextMenu::open_beside` — paint의 안전망도 같은 규칙으로 다시 놓는다(트리 우클릭: 누른 자리에 가깝고 이름이 가려지지 않는다).
- **`TabBar` 끌기 고스트**: 끄는 탭의 반투명 사본이 포인터를 따라온다(`drag_cur` · `drag_grab_dx`) — 그리드 컬럼 이동과 같은 모양.
- **`TextBox` 편집기 밖 끌기 선택**: 영역 밖(왼쪽·오른쪽)에서도 세로 위치로 **줄을 따라간다**(종전 = 글자 단위로 기어감) · 위/아래 밖은 거리에 비례한 걸음(1~12줄) · 줄 찾기는 본문을 문자열로 뜨지 않고 버퍼의 줄 표로(2 MB 1.18 ms → 0.001 ms 미만/이동) · 세 번 클릭도 줄 표로 · `bench_editor`에 끌기 시나리오.
- **`nexa-dlg` 폴더 고르기**: `PickerMode::Folder`(파일은 보이지 않는다 · 확인 = 이름 칸(친 것·누른 것) → 없으면 지금 폴더 · `confirm_folder` · 라벨 `ok_folder`/`folder_name`).
- **가린 입력란 = 비밀 값**: `set_masked` 상자는 **복사·잘라내기를 하지 않는다**(클립보드로 새는 길) · `TextBox::take_secret_text`(꺼내는 즉시 지움)/`wipe` → `EditState::wipe`(되돌리기·다시 실행 기록의 글 · 조합 중 글) → `TextBuf::wipe`(물리 바이트열 `fill(0)` + `black_box` — `unsafe` 없음 · 버퍼가 자라며 반환된 옛 할당은 닿지 않는다).
- 검증: fmt ✓ · clippy 0 · 테스트 359(`popups_stay_inside_the_host` · `open_beside_never_covers_the_target_row` · `drag_outside_left_or_right_still_follows_rows` · `masked_box_never_copies_and_wipes_on_take` · 폴더 고르기). `nexa-fs` 파일 감시 테스트가 한 번 실패 뒤 재실행 통과(타이밍 의존 — nexa-sql TODO T-161).

