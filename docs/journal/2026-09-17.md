# 2026-09-17 — 26차(win · nexa-sql 52차)

## 26차 — Toolbar 구분자/게터 · ★ `ToolDock` 툴바 그룹 도크(nexa-sql 사용자 09-17 "상단 툴바를 목적별로 그룹핑 · 영역 안 이동 · 플로팅 · 저장 · 초기화 · 라이브러리화")

- `Toolbar`: `ToolItem::separator()`(세로 선 · hover/클릭 없음 · `SEP_W` 9) · `preferred_width()`(보이는 항목 전부 + 여백) · `items()` · `item_enabled(id)`(호스트 테스트용 · nexa-sql ■ 버튼 결함) · `clear_hover()`(다른 창으로 옮겨 그릴 때 일시 상태 비움 · 포커스 규칙 ⑤).
- **`controls/tooldock.rs`**: `ToolGroup`{id · 제목 · 아이콘/구분자 계층 · `align_right`} · `ToolDock`{그룹마다 `Toolbar` **소유**(플로팅이어도) · 그립(점 2열 · hover/드래그 accent) · 그립 드래그 = 같은 정렬 이웃 중앙을 넘으면 `order` swap · **세로 22px = `DockAction::Float{id,x,y}`**(창은 호스트) · `DockLayout` 문자열 `a,b@x:y,c` parse/serialize · `apply_layout`(모르는 id 무시 · 빠진 그룹은 뒤에) · `float/dock/set_floating_pos/reset` · 항목 세터는 전 그룹 검색 · `bar_mut(id)`로 호스트가 플로팅 창에 빌려 그림 · `take_clicked`는 도크 그룹만}. 마우스 라우팅 규칙대로 Down은 커서 아래 그룹에만. 테스트 3(배치 왕복·순서 드래그/떼어 내기·클릭 라우팅/상태 공유).
- 후속(nexa-sql T-105): 아이콘·구분자 표시/순서 편집 · 플로팅 창을 도크로 끌어 붙이기 · 세로 도크.
- 검증: nexa-ctl 236 green · clippy 0 · nexa-sql 실기 캡처(도크 · 플로팅 창).

- 후속(같은 날): `Toolbar` 항목 **배지**(`set_item_badge` · 오른쪽 위 캡슐 · 색조 색) · `set_item_tip` · `ToolTone::Custom(Color)` · `ToolDock` 전달 · `ctxmenu::set_menu_icons`(우클릭 메뉴 아이콘 전역 스위치) · nexa-fs `shell::set_os_icons`(OS 아이콘 조회 즉시 None) · nexa-dlg `set_probe_chevrons`(빈 폴더 프로브) · TextBox 공백 마크 기준색 = 글자색(라이트 테마 가시성 · 테스트 `whitespace_all_mode_draws_marks_without_selection`) · nexa-font 덤프 테스트에 `·`·`$` 추가 · 풀다운 `MenuEntry::Disabled`(흐림 · hover/선택 없음 · nexa-sql 도움말 ▸ 샘플 데이터).

- 후속 2(같은 날 · nexa-sql 사용자 09-17): **TextBox 글자 세로 중앙** — 멀티라인 행 루프 `ty = y + ((lh - th)/2).max(0)`(placeholder · 색 구간 · 평문 · 공백/줄끝 마크) · 선택 반전 배경 위에 글자가 붙어 보이던 것. **`OccurrenceStyle`**(`round` · `line`/`line_alpha` · `width` · `fill`/`fill_alpha`) + `set_occurrence_style` · `paint_occurrence_box`: 상자 = 글자 좌우 2px(1px 여백 + 1px 선) · 세로 = 행 + 1 → **인접 행 상자가 선을 공유** · 선 색은 `field_bg`에 알파 미리 섞어 불투명 · 사각형은 정수 픽셀 4변(SDF 반픽셀 번짐 없음) · 둥근은 SDF. 미니맵 `dot`에 색 인자 — 선택 accent 0.6 · 동일 출현 = 출현 선 색(기본 `warn`) 0.45로 구분. nexa-fs `shell::reveal_in_file_manager`(탐색기에서 파일 선택 · 3-OS).
- 후속 3(같은 날): `EditState::selection_reversed()` · `TextBox::select_next_occurrence`가 주 선택 방향대로 추가 구간을 만든다(뒤→앞 드래그 뒤 Ctrl+D = 새 구간도 캐럿이 앞 · nexa-sql 사용자 09-17) · 테스트 `ctrl_d_keeps_reversed_caret_side` · `MINIMAP_DEFAULT_WIDTH` 80 → 160.
- 후속 4(같은 날): **Sublime 커서 규칙** — `Key::WordLeft/WordRight/SubwordLeft/SubwordRight` · `edit::word_boundary/subword_boundary` · 스마트 Home · Ctrl+Home/End · `EditState::toggle_caret`(Ctrl+클릭) · 열 선택 `column_regions` Sublime 규칙(시작 Col 못 미치는 줄 제외 · 끝 Col 줄마다 클램프) · 테스트 3(240 green).
- 후속 5(같은 날): **Auto Indent** — `AutoIndent`/`IndentRules`(sql/brackets/none) · `set_auto_indent` · Enter 유지/증가/괄호 열 정렬/indentOutdent · 닫는 토큰 내어쓰기 · 자동 공백 이탈 정리 · `on_event` 래퍼 + `on_event_inner` · 테스트(241 green).
- 후속 6(같은 날 · nexa-sql 자율 배치): 미니맵 마크 — `set_minimap_errors`(논리 줄 · danger 띠+가장자리 점 · 타이핑 시 해제) · `set_minimap_find`(찾기 범위 띠) · `set_minimap_behavior`(뷰포트 hover만 · 클릭 = 글로) · Ctrl/⌘+↑/↓ 캐럿 유지 스크롤(`vscroll`) — 241 green.
- 후속 7(같은 날): `TextBox::goto_bracket`/`expand_to_brackets`(짝 괄호 · 감싸는 쌍 · 재입력 확장) · auto indent 규칙에서 줄 주석 뒤 제외 · 테스트 `bracket_goto_and_expand`(242 green).
- 후속 8(같은 날): `TextBox::set_baseline` + `diff_lines`(prefix/suffix 트림 · LCS 상한 1500 · Added/Modified/DeletedAbove) · 거터 띠에 줄 변경 표시(RefCell 캐시 · 편집 뒤 재계산) · 테스트 `diff_marks_added_modified_deleted`.
- 후속 9(같은 날): **레인보우 괄호 코어** — `controls/pairs.rs` `PairTable`(구문 토큰 위 한 번 스캔 · `()[]{}`+인용부호(+`<>` 옵션) · 깊이/부모/짝 없음 · `pair_at`/`enclosing`/`parent`/`first_child`/`sibling`/`marks_in`) · `TextBox` `BracketOpts`/`set_bracket_opts` · `set_menu_extras`(우클릭 메뉴 추가 항목 → `EditCtxAction::Custom(id)`) · `goto_bracket_sibling/parent/child` · `auto_close`(닫힘 건너뛰기 · 선택 감싸기 · 알파벳 뒤 인용부호 제외) · `backspace_pair` · 페인트 깊이 색 순환/짝 없음 위험색+밑줄/현재 쌍 밑줄(near/always) · `Theme.rainbow` 6색 · `color_from_hex` 공개 · 테스트 `brackets_navigation_and_auto_close`(245 green · clippy 0) — nexa-sql 사용자 09-17 · 51.
- 후속 10(같은 날): CI 결함 — nexa-fs `shell.rs` `reveal_in_file_manager`가 테스트 모듈 뒤에 있어 clippy 1.98(`items_after_test_module` · ubuntu/macos 러너)에서 실패(4562500부터 · Windows 로컬은 통과) → 함수를 테스트 모듈 앞으로. 규칙: 새 항목은 `#[cfg(test)]` 모듈 **앞**에.
